<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:name=".DemoApplication"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Demo2">

        <receiver
            android:name=".searchtoolswidget.sync.WidgetScheduleReceiver"
            android:exported="false" />

        <activity
            android:name=".searchmock.ui.shortcut.SettingsActivity"
            android:exported="false" />
        <activity
            android:name=".searchmock.ui.shortcut.WeatherActivity"
            android:exported="false" />
        <activity
            android:name=".searchmock.ui.shortcut.HistoryActivity"
            android:exported="false" />
        <activity
            android:name=".searchmock.ui.shortcut.FavoritesActivity"
            android:exported="false" />
        <activity
            android:name=".searchmock.ui.SearchResultActivity"
            android:exported="false" />
        <activity
            android:name=".searchmock.ui.SearchActivationActivity"
            android:exported="false" />
        <activity
            android:name=".searchmock.ui.LauncherActivity"
            android:launchMode="singleTop"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- Mock 真正的 dp 入口，不再靠 widget 指定 Launcher 的 component。
                 MAIN/LAUNCHER 与 deeplink 分开声明，避免混淆图标入口与路由入口。 -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="demo2" android:host="app" />
            </intent-filter>
        </activity>

    </application>

</manifest>
