<resources>
    <!-- 系统会在 Application 初始化前读取 Manifest 主题，冷启必须允许启动预览。
         共同属性放在 ColdBase，供 values-v31 的平台 SplashScreen 样式继续继承。 -->
    <style name="Theme.Demo2.WidgetRouter.ColdBase" parent="android:style/Theme.Material.Light.NoActionBar">
        <item name="android:windowIsTranslucent">false</item>
        <item name="android:windowDisablePreview">false</item>
        <item name="android:windowBackground">@drawable/widget_router_starting_background</item>
        <item name="android:windowAnimationStyle">@null</item>
        <item name="android:statusBarColor">@color/widget_router_starting_background</item>
        <item name="android:navigationBarColor">@color/widget_router_starting_background</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:windowLightNavigationBar">true</item>
    </style>

    <!-- API 29～30 使用 windowBackground 的 Logo 图层作为启动预览。 -->
    <style name="Theme.Demo2.WidgetRouter.Cold" parent="Theme.Demo2.WidgetRouter.ColdBase" />

    <!-- 已经进入过 App 页面的进程，后续中转不绘制业务 Logo。
         仍是普通 Activity，可等待异步 ARouter；不使用必须立刻 finish 的 Theme.NoDisplay。 -->
    <style name="Theme.Demo2.WidgetRouter.Transparent" parent="android:style/Theme.Translucent.NoTitleBar">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowDisablePreview">true</item>
        <item name="android:backgroundDimEnabled">false</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowAnimationStyle">@null</item>
    </style>
</resources>
